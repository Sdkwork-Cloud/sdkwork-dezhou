# Config Templates

Safe runtime config templates for `sdkwork-dezhou`.

| File | Purpose |
| --- | --- |
| `dezhou.database.example.toml` | PostgreSQL connection template |
| `topology/standalone.unified-process.development.env` | Standalone dev topology env |
