services:
  dezhou-api:
    build:
      context: ../..
      dockerfile: deployments/docker/Dockerfile.dezhou-api
    ports:
      - "8096:8096"
    environment:
      SDKWORK_DATABASE_URL: postgres://sdkwork_ai_prod:sdkwork@postgres:5432/sdkwork_ai_prod
      DEZHOU_REPOSITORY_MODE: sqlx
      SDKWORK_DEZHOU_APP_ROOT: /app
    depends_on:
      - postgres

  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: sdkwork_ai_prod
      POSTGRES_PASSWORD: sdkwork
      POSTGRES_DB: sdkwork_ai_prod
    ports:
      - "5433:5432"
