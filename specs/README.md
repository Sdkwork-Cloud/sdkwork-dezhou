# Component Specs

Local component contracts for `sdkwork-dezhou`. Canonical standards live in `../sdkwork-specs/`.

- `component.spec.json` — workspace-level contract for API, database, and SDK boundaries.
- `apps/sdkwork-dezhou-pc/specs/component.spec.json` — PC application architecture contract.