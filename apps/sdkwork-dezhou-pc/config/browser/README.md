# Browser runtime config

Non-secret browser runtime env templates for `sdkwork-dezhou-pc`.

Copy the matching `runtime-env.<profile>.example.json` to your deployment surface as `runtime-env.json` when public runtime config is required.
